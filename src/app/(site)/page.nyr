Construct Module("mainPage") {
    Declare Class("main") {
        Stylesheet({
            p: "0px",
        }),
        PanoramicViewer({
            LargeScreen({
                Stylesheet({
                    p: "20px"
                })
            })
        })
    },
    Declare Class("topicCards") Deriving("flexCenterBox") {
        Stylesheet({
            flexWrap: "wrap"
        })
    }
}
